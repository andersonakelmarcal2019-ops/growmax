document.addEventListener("DOMContentLoaded", () => { const t = document.querySelectorAll(".faq-question"); t.forEach(t => { t.addEventListener("click", () => { const e = t.nextElementSibling, n = t.querySelector(".arrow"); e.classList.toggle("active"), e.classList.contains("active") ? (e.style.maxHeight = e.scrollHeight + "px", n.textContent = "-", n.style.color = "var(--accent-color)") : (e.style.maxHeight = null, n.textContent = "+", n.style.color = "inherit") }) }); const e = { threshold: .1 }, n = new IntersectionObserver(t => { t.forEach(t => { t.isIntersecting && (t.target.classList.add("visible"), n.unobserve(t.target)) }) }, e); document.querySelectorAll("section, .card, .step, .timeline-item, .method-item").forEach(t => { t.style.opacity = "0", t.style.transform = "translateY(30px)", t.style.transition = "all 0.6s ease-out", n.observe(t) }); const o = document.createElement("style"); o.innerHTML = "\n        .visible {\n            opacity: 1 !important;\n            transform: translateY(0) !important;\n        }\n    ", document.head.appendChild(o) }); document.addEventListener("DOMContentLoaded", () => { const t = document.getElementById("current-date"); if (t) { const e = new Date; t.textContent = e.toLocaleDateString("pt-BR") } });
